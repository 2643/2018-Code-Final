package org.usfirst.frc.team2643.robot;

public class Ramp {
	public static void deployRamp(boolean driveSafe, boolean opSafe, boolean opDeploy) {
		if(driveSafe && opSafe && opDeploy) {
			
		}
	}
}
